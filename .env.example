# GrünBilanz — environment variables
# Copy this file to .env and adjust values for local development.
# The DATABASE_URL below matches the Docker Compose service config.
# For production, keep credentials out of version control.

DATABASE_URL=postgresql://gruenbilanz:gruenbilanz@localhost:5432/gruenbilanz
TESSERACT_URL=http://tesseract:3001
NODE_ENV=development
REPORTS_PATH=/app/reports
